/**
 * ImageVersionEnum
 *
 */

export enum ImageVersion {

    ORIGINAL = 'ORIGINAL',      // originally received
    THUMBNAIL = 'THUMBNAIL',    // locally cached version
    MEDIUM = 'MEDIUM',          // locally cached version
    LARGE = 'LARGE'             // locally cached version

}
