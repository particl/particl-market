import { inject, named } from 'inversify';
import { Types, Core } from '../../{{deepness}}constants';
import { Logger as LoggerType } from '../../{{deepness}}core/Logger';


export class {{name.capitalize}}Listener implements interfaces.Listener {

    public static Event = Symbol('{{name.normal}}Event');

    public log: LoggerType;

    constructor(
        @inject(Types.Core) @named(Core.Logger) Logger: typeof LoggerType
    ) {
        this.log = new Logger(__filename);
    }

    public act(payload: any): void {
        this.log.info('Receive event {{name.normal}}Event', payload);
    }

}
