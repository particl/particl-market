declare module 'resources' {

    interface {{name.capitalize}} {
        id: number;
        {{#each properties}}
        {{name.camelCase}}: {{type.script}};
        {{/each}}
        {{#if hasTimestamps}}
        createdAt: Date;
        updatedAt: Date;
        {{/if}}
    }

}
